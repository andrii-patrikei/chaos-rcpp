% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{benchmark_lorenz}
\alias{benchmark_lorenz}
\title{Benchmark the compiled integrator against deSolve and pure R}
\usage{
benchmark_lorenz(
  n = 100000L,
  dt = 0.01,
  x0 = c(1, 1, 1),
  compare_steps = 500L,
  pure_r = TRUE
)
}
\arguments{
\item{n}{Number of RK4 steps.}

\item{dt}{Time step.}

\item{x0}{Initial state.}

\item{compare_steps}{Steps over which trajectories are compared.}

\item{pure_r}{Include the pure R loop (slow for large \code{n}).}
}
\value{
Data frame with columns \code{method}, \code{seconds}, \code{steps_per_second},
\code{speedup_vs_deSolve} and \code{max_abs_diff}, class \code{chaos_benchmark}.
}
\description{
Runs the same RK4 integration of the Lorenz system (same \code{dt}, same
number of steps) with four implementations and times each:
\itemize{
\item \verb{chaosrcpp fp64} and \verb{chaosrcpp fp32}: the templated C++ kernel
\item \verb{deSolve rk4}: \code{\link[deSolve:ode]{deSolve::ode()}} with \code{method = "rk4"} and an R
right-hand-side function (skipped when deSolve is not installed)
\item \verb{pure R}: an explicit RK4 loop written in R
}
All four implement the identical scheme, so the trajectories agree to
rounding over a short horizon; the column \code{max_abs_diff} reports the
largest deviation from \verb{chaosrcpp fp64} over the first \code{compare_steps}
steps as a correctness check.
}
\examples{
\donttest{
benchmark_lorenz(n = 20000)
}
}
