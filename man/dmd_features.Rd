% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmd.R
\name{dmd_features}
\alias{dmd_features}
\title{DMD summary measures as a one-row data frame}
\usage{
dmd_features(x, ..., tol = 0.01)
}
\arguments{
\item{x}{A numeric vector, a matrix or data frame of states (rows are time),
a \code{chaos_trajectory} or a \code{chaos_map}.}

\item{...}{Passed to \code{\link[=dmd]{dmd()}}.}

\item{tol}{Tolerance for \code{dmd_unit_circle_frac}.}
}
\value{
A one-row data frame.
}
\description{
The DMD counterpart of \code{\link[=rqa_features]{rqa_features()}}: a fixed set of scalars per
signal or window, ready for a feature table.
}
\details{
\itemize{
\item \code{dmd_rank}: rank used (with \code{rank = NULL}, how many modes are needed to
reach the energy threshold - a linear-algebra complexity measure).
\item \code{dmd_dom_freq}, \code{dmd_dom_growth}: frequency and growth rate of the most
energetic oscillating mode.
\item \code{dmd_max_growth}: largest growth rate (positive means unstable modes).
\item \code{dmd_spectral_radius}: largest \eqn{|\lambda|}.
\item \code{dmd_unit_circle_frac}: fraction of eigenvalues within \code{tol} of the unit
circle: 1 for periodic and quasi-periodic signals, high for finely
sampled flows, near 0 for chaotic maps.
\item \code{dmd_top_energy}: energy share of the leading mode.
\item \code{dmd_spectral_entropy}: Shannon entropy of the mode energies divided by
\code{log(rank)} (0 = one mode, 1 = energy spread evenly).
\item \code{dmd_one_step_nrmse}: relative one-step residual of the linear model; low
for regular dynamics, higher for chaotic or noisy ones. Over a logistic
map sweep \eqn{r \in [3.4, 4]} (16 delays, centred) its Spearman
correlation with the Lyapunov exponent is about 0.96, and it drops back
inside the period-3 window.
}

Frequencies and growth rates are per unit of \code{dt}, so compare features
only between signals sampled at the same rate.
}
\examples{
dmd_features(sin(seq(0, 30 * pi, length.out = 1000)), delays = 10, rank = 4)
}
