% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmd.R
\name{havok}
\alias{havok}
\alias{plot.havok}
\title{Hankel Alternative View Of Koopman (HAVOK)}
\usage{
havok(x, delays = 100L, rank = 15L, dt = NULL, var = "x")

\method{plot}{havok}(x, threshold = 2, col = "#c0392b", ...)
}
\arguments{
\item{x}{A numeric vector or a \code{chaos_trajectory} (then \code{var} selects the
measured coordinate).}

\item{delays}{Number of delays (rows of the Hankel matrix). Brunton et al.
use a window of about 0.1 time units for Lorenz.}

\item{rank}{Number of delay coordinates kept; the last is the forcing.}

\item{dt}{Sampling interval (taken from a \code{chaos_trajectory}).}

\item{var}{Column used when \code{x} is a \code{chaos_trajectory}.}

\item{threshold}{Forcing magnitude, in standard deviations, above which the
forcing is drawn as active.}

\item{col}{Colour of active-forcing points.}

\item{...}{Unused.}
}
\value{
An object of class \code{havok}: \code{A} (\eqn{(r-1) \times (r-1)}), \code{B},
\code{forcing} (\eqn{v_r}), \code{coords} (the \eqn{v} matrix), \code{t}, \code{x}
(aligned measured series), \code{singular_values}, \code{fit_r2} and settings.
}
\description{
Brunton et al. (2017) showed that a chaotic scalar series, viewed through
enough delay coordinates, is well described by a \emph{linear} system driven by
a single intermittent forcing term:
\deqn{\dot v = A v + B v_r,}
where \eqn{v_1, \dots, v_r} are the leading right singular vectors of the
Hankel matrix (eigen-time-delay coordinates) and the last one, \eqn{v_r},
plays the role of the forcing. For the Lorenz system the fitted linear
part is almost exactly skew-symmetric (a chain of oscillators) and the
forcing is mostly quiet, bursting in the saddle region where lobe
switching happens. The association is one-sided. Starting on the attractor
with the paper's settings (\code{dt = 0.001}, 100 delays, rank 15), about 93\% of
forcing bursts above 2 sd fall within 0.3 time units of a lobe switch, but
only about a third of switches come with such a burst. A burst is a
reliable warning; its absence is not an all-clear. Discard the initial
transient first, or it produces spurious bursts.
}
\details{
Derivatives of the coordinates are estimated by fourth-order central
differences and the model is fitted by ordinary least squares (the
original paper uses sparse regression; the fitted \code{A} is close to
skew-symmetric and tridiagonal either way).
}
\examples{
tr <- lorenz(n = 40000, dt = 0.002)
h <- havok(tr, delays = 50, rank = 11)
plot(h)
}
\references{
Brunton, S. L., Brunton, B. W., Proctor, J. L., Kaiser, E., &
Kutz, J. N. (2017). Chaos as an intermittently forced linear system.
Nature Communications, 8, 19.
}
\seealso{
\code{\link[=dmd]{dmd()}}
}
