% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maps.R
\name{logistic_bifurcation}
\alias{logistic_bifurcation}
\title{Bifurcation diagram of the logistic map}
\usage{
logistic_bifurcation(
  r = seq(2.5, 4, length.out = 2000),
  x0 = 0.1,
  n_transient = 500L,
  n_keep = 200L
)
}
\arguments{
\item{r}{Vector of parameter values.}

\item{x0}{Initial value. Avoid \code{0.5}: it is the critical point of the map,
so at \eqn{r = 4} it lands on the fixed point 0 after two steps.}

\item{n_transient}{Iterates discarded per \code{r}.}

\item{n_keep}{Iterates recorded per \code{r}.}
}
\value{
Data frame with columns \code{r} and \code{x}, class \code{bifurcation}.
}
\description{
For every value of \code{r} the map is iterated \code{n_transient} times to forget
the initial condition and the next \code{n_keep} iterates are recorded. With
the defaults this is 400,000 recorded points from 1.4 million iterations,
which the compiled loop does in a fraction of a second.
}
\examples{
bif <- logistic_bifurcation(seq(2.8, 4, length.out = 800))
plot(bif)
}
\seealso{
\code{\link[=logistic_lyapunov]{logistic_lyapunov()}}, \code{\link[=plot_bifurcation]{plot_bifurcation()}}
}
