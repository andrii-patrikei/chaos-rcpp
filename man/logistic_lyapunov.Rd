% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maps.R
\name{logistic_lyapunov}
\alias{logistic_lyapunov}
\title{Lyapunov exponent of the logistic map}
\usage{
logistic_lyapunov(
  r = seq(2.5, 4, length.out = 2000),
  x0 = 0.1,
  n_transient = 500L,
  n = 2000L
)
}
\arguments{
\item{r}{Vector of parameter values.}

\item{x0}{Initial value. Avoid \code{0.5}: it is the critical point of the map,
so at \eqn{r = 4} it lands on the fixed point 0 after two steps.}

\item{n_transient}{Iterates discarded per \code{r}.}

\item{n}{Iterates averaged per \code{r}.}
}
\value{
Data frame with columns \code{r} and \code{lambda}.
}
\description{
\eqn{\lambda(r) = \lim_{n\to\infty} \frac{1}{n} \sum_k \log |r (1 - 2 x_k)|}.
The exponent is negative on periodic windows, zero at bifurcation points
and positive in the chaotic regime; at \eqn{r = 4} it equals \eqn{\log 2}.
}
\examples{
ly <- logistic_lyapunov(c(3.2, 3.5, 3.9, 4))
ly
}
