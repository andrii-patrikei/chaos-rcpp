% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flows.R
\name{lorenz}
\alias{lorenz}
\title{Integrate the Lorenz system}
\usage{
lorenz(
  x0 = c(1, 1, 1),
  sigma = 10,
  rho = 28,
  beta = 8/3,
  dt = 0.01,
  n = 10000L,
  precision = c("fp64", "fp32", "dd", "mpfr"),
  thin = 1L,
  mpfr_bits = 256L
)
}
\arguments{
\item{x0}{Initial state, numeric vector of length 3.}

\item{sigma, rho, beta}{Lorenz parameters. The classic chaotic set is
\verb{sigma = 10, rho = 28, beta = 8/3}.}

\item{dt}{Time step.}

\item{n}{Number of RK4 steps.}

\item{precision}{One of \code{"fp64"}, \code{"fp32"}, \code{"dd"} or \code{"mpfr"}.}

\item{thin}{Store every \code{thin}-th step (useful for very long runs).}

\item{mpfr_bits}{Mantissa bits for the \code{"mpfr"} precision.}
}
\value{
A data frame with columns \code{t}, \code{x}, \code{y}, \code{z} and class
\code{chaos_trajectory}. Attributes \code{system}, \code{precision}, \code{dt} and \code{params}
describe the run.
}
\description{
Classical fourth-order Runge-Kutta integration of the Lorenz equations
\deqn{\dot x = \sigma (y - x), \quad \dot y = x (\rho - z) - y, \quad
      \dot z = x y - \beta z}
in a chosen floating-point precision.
}
\details{
\code{"fp32"}, \code{"fp64"} and \code{"dd"} run the same templated C++ code instantiated
with \code{float}, \code{double} and a double-double type (two doubles carrying about
106 bits, roughly 32 significant digits, built from error-free
transformations). \code{"mpfr"} runs the same RK4 formulas in arbitrary
precision through the Rmpfr package; it is the gold-standard reference but
runs as an R loop at roughly a hundred steps per second, so use it for
validation and short runs and \code{"dd"} for long high-precision runs. Because
all levels share the algorithm and the step size, differences between them
isolate the effect of rounding error. Note that even the high-precision runs
carry the truncation error of RK4 itself, so they are a reference for the
\emph{rounding} question, not the exact solution of the ODE.
}
\examples{
tr <- lorenz(n = 5000)
plot(tr)
}
\seealso{
\code{\link[=roessler]{roessler()}}, \code{\link[=precision_divergence]{precision_divergence()}}, \code{\link[=lyapunov_spectrum]{lyapunov_spectrum()}}
}
