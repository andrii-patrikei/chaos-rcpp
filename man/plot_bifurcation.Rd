% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maps.R
\name{plot_bifurcation}
\alias{plot_bifurcation}
\title{Bifurcation diagram with the Lyapunov exponent underneath}
\usage{
plot_bifurcation(bif, lyap = NULL, point_alpha = 0.08, colour = "#1f2a44")
}
\arguments{
\item{bif}{A \code{bifurcation} data frame, see \code{\link[=logistic_bifurcation]{logistic_bifurcation()}}.}

\item{lyap}{Optional data frame from \code{\link[=logistic_lyapunov]{logistic_lyapunov()}} over the same
parameter range. When \code{NULL} only the diagram is drawn.}

\item{point_alpha}{Transparency of the bifurcation points.}

\item{colour}{Point colour.}
}
\value{
A patchwork object when \code{lyap} is given and patchwork is installed,
otherwise a ggplot (or a list of two ggplots).
}
\description{
The upper panel is the bifurcation diagram, the lower one the Lyapunov
exponent over the same parameter range with the chaotic region
(\eqn{\lambda > 0}) shaded. Lining them up shows that chaos begins exactly
where the exponent crosses zero and that every periodic window is a dip
below zero.
}
\examples{
\donttest{
r <- seq(2.8, 4, length.out = 1500)
plot_bifurcation(logistic_bifurcation(r), logistic_lyapunov(r))
}
}
