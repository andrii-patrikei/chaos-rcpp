% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/divergence.R
\name{precision_divergence}
\alias{precision_divergence}
\title{The butterfly effect of rounding error}
\usage{
precision_divergence(
  x0 = c(1, 1, 1),
  sigma = 10,
  rho = 28,
  beta = 8/3,
  dt = 0.01,
  t_max = 60,
  precisions = c("fp32", "fp64", "dd"),
  reference = c("dd", "mpfr", "fp64"),
  mpfr_bits = 128L,
  thin = 1L,
  threshold = 1
)
}
\arguments{
\item{x0}{Initial state, numeric vector of length 3.}

\item{sigma, rho, beta}{Lorenz parameters. The classic chaotic set is
\verb{sigma = 10, rho = 28, beta = 8/3}.}

\item{dt}{Time step.}

\item{t_max}{Length of the integration in time units.}

\item{precisions}{Precisions to compare against the reference. Defaults
to every compiled level other than the reference.}

\item{reference}{\code{"dd"} (default), \code{"mpfr"} (needs Rmpfr) or \code{"fp64"}.}

\item{mpfr_bits}{Mantissa bits of the mpfr reference.}

\item{thin}{Store every \code{thin}-th step (useful for very long runs).}

\item{threshold}{Distance at which a trajectory is declared lost.}
}
\value{
An object of class \code{precision_divergence}: a list with
\code{trajectories} (named list of \code{chaos_trajectory} data frames including
the reference), \code{distance} (long data frame with \code{t}, \code{precision},
\code{distance}), \code{divergence_time} (first time the distance exceeds
\code{threshold}), \code{reference} and \code{params}.
}
\description{
Integrates the Lorenz system from one initial condition in several
floating-point precisions and measures how far each trajectory drifts from
a high-precision reference. Since the algorithm, the step size and the
start point are identical, the only source of disagreement is rounding.
On the chaotic attractor that disagreement grows like
\eqn{\epsilon \, e^{\lambda_1 t}}, where \eqn{\epsilon} is the unit
roundoff (about 6e-8 for fp32, 1e-16 for fp64) and \eqn{\lambda_1 \approx
0.906} is the largest Lyapunov exponent. Single precision therefore loses
the trajectory roughly twice as early as double precision, and both lose it
eventually.
}
\details{
The default reference is the compiled double-double level (\code{"dd"}, unit
roundoff about 5e-32), which is fast. \code{reference = "mpfr"} uses Rmpfr as the
gold standard instead; it is an R-level loop running at roughly a hundred
steps per second, so budget a minute for the default \code{t_max = 60}. With an
mpfr reference the dd trajectory is included in the comparison, so you get
three curves diverging at roughly \code{t = 20}, \code{40} and \code{80}.
}
\examples{
\donttest{
d <- precision_divergence(t_max = 50)
d
if (requireNamespace("ggplot2", quietly = TRUE)) plot_divergence(d)
}
}
\seealso{
\code{\link[=plot_divergence]{plot_divergence()}}
}
