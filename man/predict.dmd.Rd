% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmd.R
\name{predict.dmd}
\alias{predict.dmd}
\alias{fitted.dmd}
\title{Reconstruct or forecast with a DMD model}
\usage{
\method{predict}{dmd}(object, n_ahead = 100L, ...)

\method{fitted}{dmd}(object, ...)
}
\arguments{
\item{object}{A \code{dmd} object.}

\item{n_ahead}{Number of steps to forecast.}

\item{...}{Unused.}
}
\value{
A data frame with \code{t} and one column per original variable.
}
\description{
\code{fitted()} rebuilds the training series from the first snapshot,
\eqn{z_k = \sum_j b_j \lambda_j^{k} \phi_j}. \code{predict()} continues it
\code{n_ahead} steps past the last observation, starting from amplitudes fitted
to the final snapshot. For chaotic input expect useful forecasts only for
a fraction of a Lyapunov time: the model is linear and, with all
\eqn{|\lambda| \le 1}, it can only reproduce the quasi-periodic part of the
motion.
}
\examples{
t <- seq(0, 20, by = 0.05)
x <- sin(2 * pi * 0.4 * t) + 0.3 * sin(2 * pi * 1.1 * t)
d <- dmd(x, rank = 4, delays = 10, dt = 0.05)
head(predict(d, n_ahead = 100))
}
