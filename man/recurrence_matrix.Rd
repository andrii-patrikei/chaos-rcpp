% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rqa.R
\name{recurrence_matrix}
\alias{recurrence_matrix}
\title{Recurrence matrix}
\usage{
recurrence_matrix(
  x,
  eps = NULL,
  rr = 0.05,
  m = 1L,
  tau = 1L,
  norm = c("euclidean", "max", "manhattan")
)
}
\arguments{
\item{x}{Series, matrix or chaosrcpp object.}

\item{eps}{Threshold. When \code{NULL} it is chosen with \code{\link[=choose_eps]{choose_eps()}} so that
the recurrence rate is about \code{rr}.}

\item{rr}{Target recurrence rate used when \code{eps} is \code{NULL}.}

\item{m, tau}{Embedding dimension and delay for scalar input.}

\item{norm}{Distance norm.}
}
\value{
A logical matrix with attributes \code{eps}, \code{norm}, and class
\code{recurrence_matrix}.
}
\description{
Computes \eqn{R_{ij} = 1} if \eqn{\|x_i - x_j\| \le \epsilon}. The input can
be a scalar series (delay-embedded with \code{m} and \code{tau}), a matrix of state
vectors, or a \code{chaos_trajectory} / \code{chaos_map} object.
}
\examples{
x <- sin(seq(0, 20 * pi, length.out = 600))
R <- recurrence_matrix(x, m = 2, tau = 8, rr = 0.1)
plot_recurrence(R)
}
\seealso{
\code{\link[=rqa]{rqa()}}, \code{\link[=plot_recurrence]{plot_recurrence()}}
}
