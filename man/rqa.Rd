% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rqa.R
\name{rqa}
\alias{rqa}
\title{Recurrence Quantification Analysis}
\usage{
rqa(
  x,
  eps = NULL,
  rr = 0.05,
  m = 1L,
  tau = 1L,
  norm = c("euclidean", "max", "manhattan"),
  lmin = 2L,
  vmin = 2L,
  theiler = 1L
)
}
\arguments{
\item{x}{Series, matrix or chaosrcpp object.}

\item{eps}{Threshold. When \code{NULL} it is chosen with \code{\link[=choose_eps]{choose_eps()}} so that
the recurrence rate is about \code{rr}.}

\item{rr}{Target recurrence rate used when \code{eps} is \code{NULL}.}

\item{m, tau}{Embedding dimension and delay for scalar input.}

\item{norm}{Distance norm.}

\item{lmin}{Minimal diagonal line length counted in \code{DET}, \code{L}, \code{ENTR}.}

\item{vmin}{Minimal vertical line length counted in \code{LAM}, \code{TT}.}

\item{theiler}{Theiler window: cells with \verb{|i - j| < theiler} are ignored.
\code{1} excludes only the main diagonal.}
}
\value{
A list of class \code{rqa} with the measures, the settings and the
diagonal / vertical line length histograms.
}
\description{
Computes the standard RQA measures of a recurrence matrix in one pass
through compiled code: recurrence rate (\code{RR}), determinism (\code{DET}),
average and maximal diagonal line length (\code{L}, \code{Lmax}), divergence
(\code{DIV = 1/Lmax}), Shannon entropy of the diagonal line length distribution
(\code{ENTR}), laminarity (\code{LAM}), trapping time (\code{TT}), longest vertical line
(\code{Vmax}) and \code{RATIO = DET / RR}. Definitions follow Marwan et al. (2007).
}
\details{
Memory use is \code{N^2} bytes for \code{N} state vectors, so \code{N} up to about ten
thousand is comfortable; thin or window longer series.
}
\examples{
set.seed(1)
periodic <- sin(seq(0, 30 * pi, length.out = 1500))
noise <- rnorm(1500)
rqa(periodic, m = 3, tau = 10)
rqa(noise, m = 3, tau = 1)
}
\references{
Marwan, N., Romano, M. C., Thiel, M., & Kurths, J. (2007).
Recurrence plots for the analysis of complex systems. Physics Reports,
438(5-6), 237-329.
}
\seealso{
\code{\link[=rqa_features]{rqa_features()}}, \code{\link[=recurrence_matrix]{recurrence_matrix()}}
}
